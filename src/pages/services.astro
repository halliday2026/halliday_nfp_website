---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Services"
  description="Halliday offers data collection automation, analysis and dashboards, reporting and visualization, and custom tool development for nonprofit evaluation teams."
>
  <!-- Page header -->
  <section class="bg-[var(--color-primary-dark)] text-white py-20 lg:py-28">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-2xl">
        <p class="text-[var(--color-accent)] font-semibold text-sm uppercase tracking-widest mb-4">
          Services
        </p>
        <h1
          class="text-4xl sm:text-5xl font-bold text-white mb-6"
          style="font-family: var(--font-heading);"
        >
          Technology built around your evaluation workflow
        </h1>
        <p class="text-white/70 text-xl leading-relaxed">
          We don't sell software packages. We build solutions around how your team actually works and what your evaluation questions actually are.
        </p>
      </div>
    </div>
  </section>

  <!-- Service 1: Data Collection -->
  <section id="data-collection" class="py-16 lg:py-24 bg-[var(--color-surface)]">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-start">
        <div data-animate>
          <div class="w-14 h-14 rounded-xl bg-[var(--color-primary)]/10 flex items-center justify-center text-[var(--color-primary)] mb-6">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
            </svg>
          </div>
          <h2
            class="text-3xl font-bold text-[var(--color-text)] mb-4"
            style="font-family: var(--font-heading);"
          >
            Data Collection Automation
          </h2>
          <p class="text-[var(--color-text-muted)] text-lg leading-relaxed mb-8">
            Manual data collection is the root cause of most evaluation bottlenecks. We replace fragmented, error-prone systems with integrated digital workflows that capture clean, structured data from the moment it's created.
          </p>
          <a
            href="/contact"
            class="inline-flex items-center gap-2 bg-[var(--color-accent)] hover:bg-[var(--color-accent-hover)] text-white px-6 py-3 rounded-full font-semibold transition-colors"
          >
            Book a consultation
          </a>
        </div>

        <div data-animate data-animate-delay="200">
          <ul class="space-y-4">
            {[
              'Custom digital forms and survey tools that replace paper and Excel-based intake',
              'Integration with existing systems — databases, CRMs, and case management tools',
              'Automated data validation and cleaning to reduce errors at the source',
              'Mobile-friendly data collection for field work and community-based programs',
              'Real-time data sync so your team always has current information',
              'Consent management and data privacy controls built in from the start',
            ].map((item) => (
              <li class="flex items-start gap-3">
                <div class="w-5 h-5 rounded-full bg-[var(--color-primary)]/10 flex-shrink-0 flex items-center justify-center mt-0.5">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3 text-[var(--color-primary)]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="3">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
                  </svg>
                </div>
                <span class="text-[var(--color-text-muted)] text-sm leading-relaxed">{item}</span>
              </li>
            ))}
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Service 2: Analysis -->
  <section id="analysis" class="py-16 lg:py-24 bg-[var(--color-background)]">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-start">
        <div class="order-2 lg:order-1" data-animate data-animate-delay="200">
          <ul class="space-y-4">
            {[
              'Automated analysis pipelines aligned to your evaluation questions and logic model',
              'Real-time dashboards that track outputs and outcomes as data comes in',
              'AI-assisted qualitative data analysis — coding, theme identification, pattern recognition',
              'Statistical analysis and data modeling tailored to your methodology',
              'Longitudinal tracking to measure change over time',
              'Disaggregated data views to understand outcomes across subpopulations',
            ].map((item) => (
              <li class="flex items-start gap-3">
                <div class="w-5 h-5 rounded-full bg-[var(--color-primary)]/10 flex-shrink-0 flex items-center justify-center mt-0.5">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3 text-[var(--color-primary)]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="3">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
                  </svg>
                </div>
                <span class="text-[var(--color-text-muted)] text-sm leading-relaxed">{item}</span>
              </li>
            ))}
          </ul>
        </div>

        <div class="order-1 lg:order-2" data-animate>
          <div class="w-14 h-14 rounded-xl bg-[var(--color-primary)]/10 flex items-center justify-center text-[var(--color-primary)] mb-6">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M7 12l3-3 3 3 4-4M8 21l4-4 4 4M3 4h18M4 4h16v12a1 1 0 01-1 1H5a1 1 0 01-1-1V4z" />
            </svg>
          </div>
          <h2
            class="text-3xl font-bold text-[var(--color-text)] mb-4"
            style="font-family: var(--font-heading);"
          >
            Analysis & Insight Generation
          </h2>
          <p class="text-[var(--color-text-muted)] text-lg leading-relaxed mb-8">
            Turn months of analysis into days. We build dashboards and automated analysis pipelines tailored to your logic model and evaluation questions — so your team can spend time acting on findings, not producing them.
          </p>
          <a
            href="/contact"
            class="inline-flex items-center gap-2 bg-[var(--color-accent)] hover:bg-[var(--color-accent-hover)] text-white px-6 py-3 rounded-full font-semibold transition-colors"
          >
            Book a consultation
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Service 3: Reporting -->
  <section id="reporting" class="py-16 lg:py-24 bg-[var(--color-surface)]">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-start">
        <div data-animate>
          <div class="w-14 h-14 rounded-xl bg-[var(--color-primary)]/10 flex items-center justify-center text-[var(--color-primary)] mb-6">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 17v-2m3 2v-4m3 4v-6m2 10H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
            </svg>
          </div>
          <h2
            class="text-3xl font-bold text-[var(--color-text)] mb-4"
            style="font-family: var(--font-heading);"
          >
            Reporting & Visualization
          </h2>
          <p class="text-[var(--color-text-muted)] text-lg leading-relaxed mb-8">
            Generate funder-ready reports and stakeholder dashboards without the bottleneck. We build reporting systems that eliminate the quarterly scramble — so your team spends less time in Word and more time on programs.
          </p>
          <a
            href="/contact"
            class="inline-flex items-center gap-2 bg-[var(--color-accent)] hover:bg-[var(--color-accent-hover)] text-white px-6 py-3 rounded-full font-semibold transition-colors"
          >
            Book a consultation
          </a>
        </div>

        <div data-animate data-animate-delay="200">
          <ul class="space-y-4">
            {[
              'Automated funder report generation with customizable templates',
              'Interactive stakeholder dashboards for boards, leadership, and program staff',
              'Data visualization designed for non-technical audiences',
              'Scheduled reporting that eliminates the quarterly scramble',
              'Multi-funder reporting — produce different report formats from the same data',
              'Plain-language summaries that translate data into program decisions',
            ].map((item) => (
              <li class="flex items-start gap-3">
                <div class="w-5 h-5 rounded-full bg-[var(--color-primary)]/10 flex-shrink-0 flex items-center justify-center mt-0.5">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3 text-[var(--color-primary)]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="3">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
                  </svg>
                </div>
                <span class="text-[var(--color-text-muted)] text-sm leading-relaxed">{item}</span>
              </li>
            ))}
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Service 4: Custom Tools -->
  <section id="custom-tools" class="py-16 lg:py-24 bg-[var(--color-background)]">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-start">
        <div class="order-2 lg:order-1" data-animate data-animate-delay="200">
          <ul class="space-y-4">
            {[
              'Purpose-built evaluation management platforms',
              'Participant tracking and case management systems',
              'Custom integrations between existing tools in your stack',
              'Needs assessment and requirements gathering to ensure the right solution',
              'Iterative build process with your team testing at every stage',
              'Documentation and training so your staff can manage the system independently',
            ].map((item) => (
              <li class="flex items-start gap-3">
                <div class="w-5 h-5 rounded-full bg-[var(--color-primary)]/10 flex-shrink-0 flex items-center justify-center mt-0.5">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3 text-[var(--color-primary)]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="3">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
                  </svg>
                </div>
                <span class="text-[var(--color-text-muted)] text-sm leading-relaxed">{item}</span>
              </li>
            ))}
          </ul>
        </div>

        <div class="order-1 lg:order-2" data-animate>
          <div class="w-14 h-14 rounded-xl bg-[var(--color-primary)]/10 flex items-center justify-center text-[var(--color-primary)] mb-6">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
            </svg>
          </div>
          <h2
            class="text-3xl font-bold text-[var(--color-text)] mb-4"
            style="font-family: var(--font-heading);"
          >
            Custom Tool Development
          </h2>
          <p class="text-[var(--color-text-muted)] text-lg leading-relaxed mb-8">
            When off-the-shelf doesn't cut it, we build custom applications designed around your specific evaluation framework and data needs. No unnecessary complexity — just the right tool for the job.
          </p>
          <a
            href="/contact"
            class="inline-flex items-center gap-2 bg-[var(--color-accent)] hover:bg-[var(--color-accent-hover)] text-white px-6 py-3 rounded-full font-semibold transition-colors"
          >
            Book a consultation
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Final CTA -->
  <section class="py-16 lg:py-24 bg-[var(--color-primary-dark)] text-white">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 text-center" data-animate>
      <h2
        class="text-3xl sm:text-4xl font-bold text-white mb-5"
        style="font-family: var(--font-heading);"
      >
        Not sure where to start?
      </h2>
      <p class="text-white/70 text-lg mb-8">
        A free workflow review is the best way to figure out which service area creates the most leverage for your team. No commitment, no pressure.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href="/contact"
          class="inline-flex items-center justify-center gap-2 bg-[var(--color-accent)] hover:bg-[var(--color-accent-hover)] text-white px-7 py-4 rounded-full font-semibold transition-colors"
        >
          Book a Free Consultation
        </a>
        <a
          href="/assessment"
          class="inline-flex items-center justify-center gap-2 border-2 border-white/40 hover:border-white text-white px-7 py-4 rounded-full font-semibold transition-colors hover:bg-white/10"
        >
          Take the Assessment First
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
